import {
    ExtensionSigner
} from './ExtensionSigner';

import {
    isExtensionAvailable,
    enableExtensions,
    InjectedAccount,
    enableMetaMask,
    allMetamaskAccounts,
} from './Extension';

export {
    isExtensionAvailable,
    enableExtensions,
    ExtensionSigner,
    enableMetaMask,
    allMetamaskAccounts,
}

export type {
    InjectedAccount
}
